import css from '@/lib/css';

const Styled = {
  field: css({
    display: 'grid',
    marginBottom: '10px',
  }),

  label: css({
    fontSize: ' 15px',
    fontWeight: '500',
    lineHeight: '35px',
  }),

  message: css({
    fontSize: '13px',
    marginLeft: '10px',
    opacity: '0.8',
    color: 'red',
  }),
};

export default Styled;
